<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.green.plants.dao.ItemDAO">
	<insert id="insertItem">
	insert into item(seller_member_id, `name`, price, `count`, contents, `option`, `file`)
	values(#{item.seller_member_id}, #{item.name}, #{item.price}, #{item.count}, #{item.contents}, #{item.option}, #{item.file})
	</insert>
	
	<select id="selectItem" resultType="kr.green.plants.vo.ItemVO">
	select * from item where valid = 'I'
	</select>
</mapper>